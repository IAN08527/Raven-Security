[package]
name = "raven_graph_cli"
version = "0.1.0"
description = "Headless proof of the Raven graph engine (Backlog #4)"
edition = "2021"

[dependencies]
raven-core = { path = "../../src-tauri/core" }
tokio = { version = "1", features = ["full"] }
serde_json = "1"
dotenvy = "0.15"

[workspace]
